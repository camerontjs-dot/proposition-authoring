# Naturalistic Shadow Batch 001

This run exercises the paired ClaimGate + EvidenceGate shadow prototype without changing downstream CAL Pipeline behavior.

Subjects:

- observed A1A-C1 claim `Women had a higher rate than Men.` with the original evidence world explicitly unavailable rather than fabricated;
- a synthetic matched-evidence control for the same claim text;
- a synthetic mismatched-evidence control for the same claim text;
- the frozen ClaimGate DECLARED, NOT_NEEDED, and ABSTAINED front-door fixtures.

The original A1A request identity retained from the local pipeline record is `850799d128efb3f3dadd76d79294b37856e1f7f33539445c88d4c240e7f36e71`. The 12 exact source bytes are not present in GitHub and are not reconstructed here.

Outputs are generated by `.github/workflows/research-paired-gates-shadow-batch.yml` and uploaded as the `paired-gates-shadow-batch` artifact. The synthetic controls are measurement instruments only and must not be interpreted as reconstructions of the A1A evidence corpus.
